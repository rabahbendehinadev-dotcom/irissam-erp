/**
 * Ambulances — production stub.
 * The route /ambulances is permanently redirected to / in App.tsx.
 * MockRepository is not used; this component is never rendered in production.
 */
export default function Ambulances() {
  return null;
}
